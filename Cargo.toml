[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
askama = { version = "*" }
axum = { version = "*" }
axum-login = { version = "*" }
dotenv = { version = "*" }
password-auth = { version = "*" }
serde = { version = "*", features = ["derive"] }
sqlx = { version = "*", features = ["mysql", "runtime-tokio"] }
time = { version = "*" }
tokio = { version = "*", features = ["rt-multi-thread", "signal"] }
tower-http = { version = "*", features = ["trace"] }
tracing = { version = "*" }
tracing-subscriber = { version = "*", features = ["env-filter"] }
